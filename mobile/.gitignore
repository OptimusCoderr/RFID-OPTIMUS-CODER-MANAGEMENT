node_modules/
.expo/
dist/
web-build/
# Generated by `expo prebuild` (Continuous Native Generation) — regenerated
# from app.json + the config plugins before every dev-client/EAS build, not
# hand-edited, so it isn't committed.
/android/
/ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.env
.env.local
